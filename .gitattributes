# Календарь iCalendar по стандарту RFC 5545 требует переводов строк CRLF.
# Без этого строгие клиенты (Outlook, часть версий Apple Календаря) могут
# отказаться читать подписку. Запрещаем git нормализовать этот файл.
*.ics -text
